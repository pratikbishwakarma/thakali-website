version: '3.8'

services:
  thakali-site:
    image: thakali-site
    build:
      context: .
      dockerfile: Dockerfile
    container_name: thakali-container
    ports:
      - "3000:80"
    restart: always
